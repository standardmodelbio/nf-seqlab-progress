plugins {
    id 'nf-seqlab-progress@0.1.0'
}

params {
    outdir = "${projectDir}/results"
    progress_mode = 'plain'
    progress_refresh_seconds = 0.1
    progress_max_active_files = 2
}

process {
    executor = 'local'
}

